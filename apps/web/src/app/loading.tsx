export default function Loading() {
  return (
    <main className="grid min-h-screen place-items-center" aria-live="polite">
      <p className="text-sm text-slate-600">Loading TransitOps…</p>
    </main>
  );
}
